Interaction Finder:
- add an X so you can delete a character rather than having to clear everything
- result count
- make each result have its own link

Character Pages:
- add an interactions tab to character pages that automatically show interactions that include that character
